make a card view where you can view and edit a single card, see it in card form in the corner, have it work like a sheet of notebook paper besides that where you can have a long version and an index card version. 

color pallete


constraint solver!!!!!!!!!!


let parent-id = cursor something

every one is pushed object into array -->
{ 
    id: 12345,
    parent-id: null,
    type: card,
    index: itemsList.length,
    indent-count: 0,
    card-title: null,
    card-content: null,
    page-content: null
}